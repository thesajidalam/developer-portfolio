# Supabase project (create in supabase.com/dashboard)
# Project URL & publishable key are public-safe (client env vars, prefixed NEXT_PUBLIC_).
NEXT_PUBLIC_SUPABASE_URL=https://mfajabpklpyaztnyzlig.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_1obfDJUkRZGtF0-tglZMyQ_-xDgxmB8

# SERVICE ROLE KEY — SECRET. Server-only. NEVER expose in client bundles.
# Chargeable/privileged: use it ONLY in server code (API routes, seed).
SUPABASE_SERVICE_KEY=replace-me-with-your-sb_secret_key

# Admin API key — SECRET. Protects admin endpoints + embedded score token.
ADMIN_API_KEY=devfolio-admin-secret-change-me

# Public site origin (used for social cards + canonical URLs).
NEXT_PUBLIC_BASE_URL=https://gitdevfolio.vercel.app
